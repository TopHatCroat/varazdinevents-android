<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/menu_all_events"
            android:icon="@android:drawable/ic_menu_agenda"
            android:title="@string/all_events_menu" />

    </group>
    <item android:title="@string/organisers">
        <menu>
            <item
                android:id="@+id/menu_create_event"
                android:icon="@android:drawable/ic_menu_edit"
                android:title="@string/new_event_description"
                />
            <item
                android:id="@+id/menu_host"
                android:icon="@android:drawable/ic_menu_my_calendar"
                android:title="@string/menu_host"
                />

        </menu>
    </item>
    <item android:title="@string/application">
        <menu>
            <item
                android:id="@+id/menu_settings"
                android:icon="@android:drawable/ic_menu_preferences"
                android:title="@string/settings" />
            <item
                android:id="@+id/menu_about"
                android:icon="@android:drawable/ic_menu_info_details"
                android:title="@string/about" />
            <item
                android:id="@+id/menu_login"
                android:icon="@android:drawable/ic_menu_myplaces"
                android:title="@string/login" />
            <item
                android:id="@+id/menu_logout"
                android:icon="@android:drawable/ic_menu_close_clear_cancel"
                android:title="@string/logout" />
        </menu>
    </item>
</menu>